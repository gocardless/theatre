---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: releases.deploy.gocardless.com
spec:
  group: deploy.gocardless.com
  names:
    kind: Release
    listKind: ReleaseList
    plural: releases
    singular: release
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          config:
            description: ReleaseConfig the release configuration
            properties:
              revisions:
                additionalProperties:
                  properties:
                    id:
                      description: ID is the unique identifier of the revision (e.g.,
                        commit SHA, image digest, chart version)
                      type: string
                    metadata:
                      description: Metadata contains additional optional information
                        about the revision
                      properties:
                        author:
                          description: Author is the author of the commit, if available.
                            The field is optional.
                          type: string
                        branch:
                          description: Branch is the branch of the commit, if available.
                            The field is optional.
                          type: string
                        message:
                          description: Message is the message of the commit, if available.
                            The field is optional.
                          type: string
                        tags:
                          description: Tags are additional tags associated with this
                            revision
                          items:
                            type: string
                          maxItems: 10
                          type: array
                      type: object
                    source:
                      description: Source identifies where this revision comes from
                        (e.g., repository URL, registry URL)
                      type: string
                    type:
                      description: Type specifies the kind of revision source (git,
                        container_image, helm_chart)
                      type: string
                  required:
                  - id
                  type: object
                description: |-
                  Revisions is a map of revision names to their specifications
                  Each source must be unique across all revisions
                maxProperties: 10
                minProperties: 1
                type: object
              targetName:
                description: TargetName is a namespace-unique identifier for this
                  release target
                type: string
            required:
            - revisions
            - targetName
            type: object
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          status:
            description: Status defines the observed state of Release
            properties:
              healthStatus:
                default: Unknown
                description: HealthStatus indicates whether the release is healthy
                  or not, as determined by an external monitoring system.
                enum:
                - Healthy
                - Unhealthy
                - Unknown
                type: string
              healthStatusLastChecked:
                description: HealthStatusLastChecked is the last time the health status
                  was checked by the external system.
                format: date-time
                type: string
              history:
                description: History is a list of previous statuses of the release.
                items:
                  properties:
                    healthStatus:
                      default: Unknown
                      description: HealthStatus indicates whether the release is healthy
                        or not, as determined by an external monitoring system.
                      enum:
                      - Healthy
                      - Unhealthy
                      - Unknown
                      type: string
                    healthStatusLastChecked:
                      description: HealthStatusLastChecked is the last time the health
                        status was checked by the external system.
                      format: date-time
                      type: string
                    lastAppliedTime:
                      description: LastAppliedTime is the last time the release was
                        applied to the cluster.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable message indicating
                        the state of the release.
                      type: string
                    phase:
                      description: |-
                        Phase is the current phase of the release. Active indicates the release
                        is currently live in the cluster, Inactive indicates the release is no
                        longer the latest release.
                      enum:
                      - Active
                      - Inactive
                      type: string
                    supersededBy:
                      description: SupersededBy is the name of the release that superseded
                        this release.
                      type: string
                    supersededTime:
                      description: SupersededTime is the time when this release was
                        superseded.
                      format: date-time
                      type: string
                  required:
                  - phase
                  type: object
                maxItems: 50
                type: array
              lastAppliedTime:
                description: LastAppliedTime is the last time the release was applied
                  to the cluster.
                format: date-time
                type: string
              message:
                description: Message is a human-readable message indicating the state
                  of the release.
                type: string
              phase:
                description: |-
                  Phase is the current phase of the release. Active indicates the release
                  is currently live in the cluster, Inactive indicates the release is no
                  longer the latest release.
                enum:
                - Active
                - Inactive
                type: string
              supersededBy:
                description: SupersededBy is the name of the release that superseded
                  this release.
                type: string
              supersededTime:
                description: SupersededTime is the time when this release was superseded.
                format: date-time
                type: string
            required:
            - phase
            type: object
        required:
        - config
        type: object
    served: true
    storage: true
    subresources:
      status: {}
