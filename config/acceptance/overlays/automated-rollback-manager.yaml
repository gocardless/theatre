apiVersion: apps/v1
kind: Deployment
metadata:
  name: automated-rollback-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      controller: automated-rollback-manager
      group: deploy.crd.gocardless.com
  template:
    metadata:
      labels:
        controller: automated-rollback-manager
        group: deploy.crd.gocardless.com
    spec:
      containers:
        - command:
            - /usr/local/bin/automated-rollback-manager
          args:
            - --metrics-address=0.0.0.0
          image: theatre:latest
          imagePullPolicy: Never
          name: manager
          resources:
            limits:
              cpu: 100m
              memory: 256Mi
