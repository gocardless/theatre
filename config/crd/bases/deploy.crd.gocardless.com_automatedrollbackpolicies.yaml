---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: automatedrollbackpolicies.deploy.crd.gocardless.com
spec:
  group: deploy.crd.gocardless.com
  names:
    kind: AutomatedRollbackPolicy
    listKind: AutomatedRollbackPolicyList
    plural: automatedrollbackpolicies
    shortNames:
    - arbp
    singular: automatedrollbackpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetName
      name: Target
      type: string
    - jsonPath: .spec.trigger.conditionType
      name: Trigger_Condition
      type: string
    - jsonPath: .spec.trigger.conditionStatus
      name: Trigger_When
      type: string
    - jsonPath: .status.conditions[?(@.type=="Automated")].status
      name: Automated
      type: string
    - jsonPath: .status.conditions[?(@.type=="Automated")].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=="Automated")].message
      name: Message
      priority: 10
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AutomatedRollbackPolicy is the Schema for the automatedrollbackpolicies
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of AutomatedRollbackPolicy
            properties:
              enabled:
                default: false
                description: Enabled controls whether automated rollbacks are active.
                type: boolean
              rollbackTemplate:
                description: |-
                  RollbackTemplate groups all configuration applied to the Rollback resource
                  created by the controller.
                properties:
                  metadata:
                    description: Metadata fields applied to the created Rollback resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations to add to the Rollback resource.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels to add to the Rollback resource.
                        type: object
                    type: object
                  spec:
                    description: Spec fields applied to the created Rollback resource's
                      spec.
                    properties:
                      deploymentOptions:
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                        description: DeploymentOptions contains additional rollback
                          provider-specific options.
                        type: object
                    type: object
                type: object
              targetName:
                description: |-
                  TargetName identifies which releases this policy applies to,
                  matching Release.config.targetName.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: TargetName is immutable
                  rule: self == oldSelf
              trigger:
                description: Trigger defines the Release condition that triggers a
                  rollback.
                properties:
                  conditionStatus:
                    default: "True"
                    description: ConditionStatus is the status value that triggers
                      a rollback.
                    enum:
                    - "True"
                    - "False"
                    type: string
                  conditionType:
                    default: RollbackRequired
                    description: ConditionType is the Release status condition type
                      to watch.
                    type: string
                type: object
            required:
            - enabled
            - targetName
            type: object
          status:
            description: Status defines the observed state of AutomatedRollbackPolicy
            properties:
              conditions:
                description: Conditions represent the latest observations of the policy's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastAutomatedRollbackTime:
                description: LastAutomatedRollbackTime is when the last automated
                  rollback was created.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
