---
apiVersion: v1
kind: Pod
metadata:
  name: app
  annotations: {
    "envconsul-injector.vault.crd.gocardless.com/configs": "app:config/app.yaml"
  }
spec:
  containers:
    - name: app
      command:
        - echo
        - inject
        - only
