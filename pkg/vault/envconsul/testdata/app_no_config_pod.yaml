---
apiVersion: v1
kind: Pod
metadata:
  name: app
  annotations: {
    "envconsul-injector.vault.crd.gocardless.com/configs": "app"
  }
spec:
  containers:
    - name: app
      command:
        - echo
        - inject
        - only
      volumeMounts:
        - name: app-volume
          mountPath: /app/path
